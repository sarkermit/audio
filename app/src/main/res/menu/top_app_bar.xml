<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/menuRestore"
        android:icon="@drawable/ic_restore"
        android:title="@string/txt_restore"
        android:visible="false"
        android:contentDescription="@string/txt_restore"
        app:showAsAction="ifRoom" />

    <item
        android:id="@+id/menuDownload"
        android:icon="@drawable/ic_download"
        android:title="@string/txt_download"
        android:visible="false"
        android:contentDescription="@string/txt_download"
        app:showAsAction="ifRoom" />


</menu>